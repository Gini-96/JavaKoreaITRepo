package javabasic.oop.inheritance;

// 상속 받는 클래스
// 하위클래스(sub) = 자식클래스(child) = 파생클래스(derived)

// Parent 의 참조 범위는 Object ~ Parent
// Child 의 참조 범위는 Object ~ Parent ~ Child
// 항상 하위 클래스 객체의 참조범위는 상위클래스 객체의 참조범위보다 크다
// 모든 클래스들은 Object 에서부터 파생되기 때문에 표시가 되어 있지 않더라도 Object 를 포함 하고 있다
public class Child extends Parent {

	
}
